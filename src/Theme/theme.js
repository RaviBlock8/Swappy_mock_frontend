import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#166ee7", light: "#700688" },
    secondary: { main: "#48A8E8" },
    selected: { main: "#700688" },
  },
});

export default theme;
